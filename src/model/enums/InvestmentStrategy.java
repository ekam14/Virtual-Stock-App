package model.enums;

/**
 * This enum represents possible investment strategies.
 */
public enum InvestmentStrategy {
  DOLLAR_COST_AVERAGING("DOLLAR_COST_AVERAGING"), FIXED_AMOUNT("FIXED_AMOUNT");

  /**
   * Initializes enum values to a string.
   *
   * @param value string value.
   */
  InvestmentStrategy(String value) {
    // pass;
  }
}
